// Site-wide configuration values that a human may need to change.
//
// Ko-fi voluntary-support link. This is a plain external link — no SDK, no
// iframe, no widget, no cookies. This is the ONLY place the URL is defined;
// everything links to `KOFI_URL`.
export const KOFI_URL = 'https://ko-fi.com/pole2';
