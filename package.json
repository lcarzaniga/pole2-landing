{
  "name": "pole2-landing",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Pole² — landing / waiting-list page. A calm public home, built with Astro (static, zero-JS by default).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "check": "astro check",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
